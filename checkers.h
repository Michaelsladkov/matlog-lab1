#ifndef _CHECKERS
#define _CHECKERS

bool variable(char** stream);
bool inversion(char** stream);
bool conunction(char** stream);
bool disunction(char** stream);
bool expression(char** stream);

#endif